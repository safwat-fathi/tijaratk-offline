import LoginForm from "@/app/(dashboard)/merchant/(auth)/_components/auth/login-form";

export const metadata = {
	title: "Merchant Login",
	description: "Login to your merchant account",
};

export default function LoginPage() {
  return <LoginForm />;
}
